/**
 * Twitter/X share card — the SAME generated brand card as the OpenGraph
 * image (social-acquisition readiness v1). One renderer, two metadata
 * slots: the card declared `summary_large_image` long before any image
 * existed, and both tags must point at the identical honest composition.
 */
export { default, alt, size, contentType } from "./opengraph-image";
