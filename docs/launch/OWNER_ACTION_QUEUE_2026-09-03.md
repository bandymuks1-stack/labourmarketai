# OWNER ACTION QUEUE — 2026-09-03 (FIRST REAL ECOSYSTEM USE)

> Supersedes `OWNER_ACTION_QUEUE_2026-09-02.md` as the consolidated list. Only
> actions the agent genuinely cannot perform. Everything else is autonomous
> work already in flight or queued (see `RESUME_CHECKPOINT_2026-09-03.md`).
> Nothing here blocks the tracks listed in the last column — they proceed in
> parallel.

| # | OWNER ACTION | WHY REQUIRED | EXACT STEPS | EXPECTED TIME | WHAT TO RETURN TO AGENT | WHAT CONTINUES IN PARALLEL |
|---|---|---|---|---|---|---|
| 1 | ~~Approve + apply RED batch A~~ **APPLIED 2026-09-03** (owner: "Apply batch 2026-09-03 A+B+C"; ledger 20260903105726 + 20260903105753; #1448 ready, auto-merge): `20260903100000_public_vacancy_supply_counts_v1` and `20260903101000_agency_candidate_offer_decision_v1` | Both replace/create SECURITY DEFINER functions and carry GRANT/REVOKE (+ one CHECK widening) — RED by the merge envelope; no self-apply | 1. Open the draft PR, read the two SQL files (they are the whole change). 2. Reply "Apply batch 2026-09-03 A" in chat (or apply yourself via Supabase MCP `apply_migration`, then say so). 3. Mark the PR ready. | 10 min read | the words "Apply batch 2026-09-03 A" (or "applied") | worker/employer/student/education tracks; the agency UI is built with honest `needs-migration` degradation until applied |
| 1b | ~~Approve + apply RED batch B~~ **APPLIED 2026-09-03** (ledger 20260903105839; #1454 ready): `20260903120000_education_programs_cohorts_v1` — education programmes / cohorts / cohort members (institution → programme → cohort → learner) + `count_public_vacancies_by_profession_v1` (real demand per profession) | New tables + grants + SECURITY DEFINER commands — RED by the envelope; no self-apply | Read the one SQL file; reply "Apply batch 2026-09-03 B" (or apply via Supabase MCP and say so). Can be approved together with 1. | 10 min | the sentence | the institution programme/cohort UI ships with honest `needs-migration` degradation and lights up on apply |
| 1c | ~~Approve + apply RED batch C~~ **APPLIED 2026-09-03** (ledger 20260903105902; #1456 ready): `20260903140000_institution_learner_outcomes_v1` — ONE aggregate function (counts only, suppressed below 5 learners) so an institution sees whether its programme leads somewhere, with no access to learner records | SECURITY DEFINER + grant — RED by the envelope | Read the one SQL file; reply "Apply batch 2026-09-03 C" (can be approved together with 1 and 1b) | 5 min | the sentence | Lane B pilot runs without it (B10 shows "later slice") |
| ~~VERCEL-1~~ | **CLOSED 11:47 UTC without owner action** — the rolling window freed a slot and `8d3e7dec` (batch C merge, contains A+B+C) deployed to production; health `build=8d3e7dec`. Kept for the record: production deploy was rate-limited (Vercel Hobby): every push since 10:51 UTC returns "Deployment rate limited — retry in 24 hours".** Production still serves build `342d225b`; `main` is ahead by the RED batch A merge (#1448, `6255e28e`) and, once merged, B/C. The DB migrations are applied and additive, so the old build runs correctly against them — nothing is broken, the new agency-decision and programmes UI is simply not live yet. | Hobby plan deploy quota exhausted by today's branch pushes (each push = one deployment) | Either (a) wait for the window and press **Redeploy** on the latest `main` deployment in the Vercel dashboard (or push any commit to `main`), or (b) upgrade the Vercel project to Pro. Claude Code cannot log in to Vercel. | 2 min | "redeployed" / "upgraded" | Lane A/B pilots on the NEW UI (agency decision buttons, programmes/cohorts) wait for this; everything DB-side is ready |

> **Final security review done 2026-09-03 (owner-requested, before approval).** All three batches were reviewed for SECURITY DEFINER + `search_path`, GRANT/REVOKE, RLS, tenant/institution/agency authorization, CHECK widening, anon/authenticated reach, data-change/loss risk and rollback. Three fixes were made and pushed (each PR body carries the detail): A — `count_public_vacancies_v1` re-states `work_mem = 64MB` (CREATE OR REPLACE drops SET config; verified on prod) and uses a deterministic `not exists` fallback; B — covering index for the per-profession count (7.2 s → 176 ms on prod) and the grant narrowed to **authenticated only** (anon allowlist entry removed); C — suppression threshold 3 → 5. Guards green in all three worktrees. Verdict: **SAFE TO APPROVE — one sentence covers all three: "Apply batch 2026-09-03 A+B+C".**
| ~~0~~ | ~~Approve + apply RED batch 2026-09-03 D — #1457~~ **APPLIED 2026-09-03 13:04 UTC** (owner: "Apply batch 2026-09-03 D"; prod ledger `20260903130400`; readback: all three policies route through the helpers, anon EXECUTE false / authenticated true, no table write grants, anon definer set still 8, manager 0/0/0 → 1/1/1 after its own writes, learner exactly own membership → cohort → programme, outsider 0/0/0, anon 42501; residue 0; #1457 marked ready, auto-merge armed). Original row: `20260903150000_education_rls_recursion_fix_v1` (draft, `needs-human-gate`). **PRODUCTION DEFECT, pilot-blocking for Lane B:** the three batch-B SELECT policies reference each other, so **every** authenticated read of `education_programs` / `education_cohorts` / `education_cohort_members` fails with `42P17 infinite recursion detected in policy` (manager, learner, outsider alike — read back on prod 2026-09-03 ~15:20 UTC). The institution programmes section therefore renders "—" and never offers the create form (B2/B3/B6 unreachable); the student cohort view (#1458, merged) shows no cohort. No data is wrong; the definer writes still work. The fix keeps the SAME readers and evaluates membership through three SECURITY DEFINER helpers (authenticated-only EXECUTE) so the policies no longer re-enter each other. **Proven on prod in a rolled-back transaction** (manager 1/1/1, learner exactly own rows, outsider 0/0/0, learner cannot write, anon refused) and forward/down/forward on the local stack. | DROP/CREATE POLICY + SECURITY DEFINER + GRANT/REVOKE — RED by the envelope; no self-apply | 1. Open #1457, read the one SQL file + rollback (the PR body carries the full SQL and the proof table). 2. Reply **"Apply batch 2026-09-03 D"** (or apply yourself via Supabase MCP `apply_migration` and say so). | 5 min | the sentence | everything; Lane B pilot cannot create programmes/cohorts until applied |
| DEV-1 | **Free the local Supabase ports on the dev machine** (not a product gate). Windows reserves TCP **54290–54389** (`netsh interface ipv4 show excludedportrange protocol=tcp`), so `npx supabase start` fails to bind 54321/54322 and the agent cannot run authenticated browser checks against the local stack (DB work via `docker exec` still works). | Windows system setting — admin only | As administrator: `net stop winnat` → `net start winnat` (frees the dynamic block), then `npx supabase start`; if it comes back reserved, `netsh int ipv4 add excludedportrange protocol=tcp startport=54321 numberofports=2` right after a reboot, or move the ports in `supabase/config.toml` (tell the agent). Optional, same visit: Supabase → Project Integrations → GitHub → raise the concurrent-branches limit (the "Supabase Preview" check on PRs is cancelled at the limit — informational, not required). | 3 min | "ports free" | everything (only local UI verification is affected). **Workaround in place (evening 2026-09-03):** the agent runs the local stack on 553xx through an UNCOMMITTED `supabase/config.toml` change + an uncommitted `dev-local` entry in `.claude/launch.json`; authenticated local walks work again. Freeing the range lets those two local edits be dropped. |
| 2 | **Approve + apply batch 2026-09-02** (unchanged): #1430, #1436, #1426, #1440 | Same RED class (already reviewed; SQL in each PR) | Reply "Apply batch 2026-09-02" | 10 min | the sentence | all tracks |
| 3 | **G-1 one real-inbox test** — now also the first real walk of the new first-run router | Only a real mailbox proves delivery (Resend SMTP is live; every test box bounces) | On a phone, private window → `https://labourmarket.ai/lt/auth/signup` → e-mail + password with a real address never registered → open the mail → tap the link → you land on the new question "What did you come here to do?" → pick one intent and finish. Three screenshots (inbox, the intent screen, where you land). Full script: `docs/human-gates/email-delivery-gate.md` (bottom) | 4 min | the screenshots + rough delivery time + which intent you picked | everything |
| 4 | **LinkedIn app** (G-2a) | Developer-console app + secret can only be created by the page owner | https://www.linkedin.com/developers/apps → *Create app* (needs the LabourMarket.ai company page) → Products: add **Sign In with LinkedIn using OpenID Connect** → Auth tab → *Authorized redirect URLs*: `https://gorgitwvdzxbnaxhrsrw.supabase.co/auth/v1/callback` → copy **Client ID** + **Primary Client Secret** → Supabase dashboard → Authentication → Sign In / Providers → **LinkedIn (OIDC)** → enable, paste both, Save (scopes stay `openid profile email`) | 10–15 min | "LinkedIn on" (never the secret) — the button appears within 5 min, no deploy | everything; the agent then runs the 11-leg proof (`tests/e2e/social-auth-matrix.spec.ts`) with one owner sign-in |
| 5 | **Meta / Facebook app** (G-2b) | Same — app + secret + Live switch are owner-only | https://developers.facebook.com/apps → *Create app* → use case "Authenticate and request data from users with Facebook Login" → add **Facebook Login** → Facebook Login → Settings → *Valid OAuth Redirect URIs*: the same Supabase callback URL → App settings → Basic: copy **App ID** + **App Secret**, set Privacy Policy URL `https://labourmarket.ai/lt/legal/privacy`, category, icon → switch the app to **Live** → Supabase → Authentication → Providers → **Facebook** → enable, paste both, Save (`email` + `public_profile` need no App Review) | 15–20 min | "Facebook on" | everything |
| 6 | **VPS-1 Agentai scheduler state** | `ssh agentai-vps` is denied to the agent by the permission layer; the host and secrets are yours | Run: `ssh agentai-vps 'cd /srv/agentai && docker compose -f docker-compose.production.yml ps && docker compose -f docker-compose.production.yml exec -T scheduler sh -c "echo PROFILE=$AGENTAI_DEPLOYMENT_PROFILE; cat runtime/scheduler/heartbeat.json"'` and paste the output — or grant the agent `ssh agentai-vps` read access in this session | 2 min | the pasted output (or the grant) | radar integration design (Track E) proceeds on the contract file; the 12:00 job is added once the profile is known |
| 7 | **TG-1 Telegram channel** | Creating/claiming a public channel is a real-world account action | In Telegram: New Channel → name "LabourMarket.ai" → public link `t.me/labourmarketai` (or the nearest free) → add the existing owner bot (`@BotFather` token already in `OWNER_TELEGRAM_BOT_TOKEN`) as an **administrator** with "Post messages" | 5 min | the channel link | the distribution job and channel-discovery registry are prepared without it |
| 8 | **G-14 admin verify** | One admin click on the one admin account | Admin → Company verification → `E2E Walker UAB` → Verify | 1 min | "done" | employer proof re-run |
| 9 | **G-16 waiver line** on #1433 | Product-gate waiver is an owner sentence | Reply "Waive JSON-LD gate for /jobs (#1433)" | 1 min | the sentence | — |
| 10 | **G-7 / G-8 Stripe** (before commercial launch only) | Price confirmation + live keys are owner-held | "Set A" on #1441; Stripe live keys + webhook secret into Vercel; approve #1441 | 20 min | "Set A confirmed", "keys set" | not needed for FIRST REAL ECOSYSTEM USE |
| 11 | **L3 rollback drill** (before commercial launch) | Vercel dashboard action | Deployments → previous → *Promote to production* → confirm health → re-promote | 5 min | "drilled" | — |

## Not owner actions (autonomous, in flight)

P0-1 covering index apply after #1446 merges · P2-1 apex 404 (#1446) · first-run router (#1447) · agency client-side decision UI with honest degradation · student / education first-value slices · time-to-first-value instrumentation · pilot checklists · Telegram distribution job scaffold · capability-contract regeneration for Agentai.
