# Owner-review evidence — provider-free location (PR #484)

Generated by `scripts/evidence-location-provider-free.ts` from the real pure model. No Google, no paid provider, no network, no API key.

### Automatic location (browser geolocation)
```
source: auto
where: 54.6872, 25.2797
radius: 25 km
has browser coordinates: true
usable for job search (no provider): true
```

### Manual location (country + city/region, no geocoding)
```
source: manual
where: Vilnius, LT
radius: 50 km
has browser coordinates: false
usable for job search (no provider): true
```

### Geolocation denied → manual fallback still works
```
source: manual
where: Warszawa, PL
radius: 100 km
has browser coordinates: false
usable for job search (no provider): true
```

### Radius selection
```
options: 10 / 25 / 50 / 100 km   (default 25 km)
normalizeRadius(30) -> 25 ; normalizeRadius(60) -> 50
```

### Provider-free continuation
```
manual country-only usable without coordinates: true
no Google Maps key required: true (the model imports no provider)
```
