# W1–W22 CURRENT STATE MATRIX (recounted 2026-08-06, main `4f61a22b`)

Verdict: `W1_W22_PROGRAM_RECOUNTED_READY_FOR_NEXT_INCOMPLETE_W`

Read-only recount per the owner directive 2026-08-06 §10, verified against
CURRENT main (every headline claim spot-checked in code/migrations/ledger —
NOT copied from the 2026-08-03 baseline, which is 3 days and ~14 merges
stale). Canonical prior sources: `docs/audits/premium-rebuild-live-state.md`
(stage ledger), `docs/audits/post-merge-production-readiness-baseline-2026-08-03.md`
§6 (full table). The M-P0-1…M-P0-8 multi-org train
(`docs/architecture/MULTI_ORGANIZATION_STRUCTURAL_TRAIN.md`) is a SUCCESSOR
programme that closed several W blockers — credited per-W below.

Classifications: `DONE` | `PARTIAL` | `SUPERSEDED` | `REMAINING` |
`OWNER_GATED` | `NOT_LAUNCH_CRITICAL` | `DEFERRED_LONG_TERM`.

## Cross-cutting blockers (gate several W rows at once)

1. **`PROD_QA_*` accounts unprovisioned** (`docs/audits/evidence/premium-rebuild/prod-qa-account.md`)
   → NO authenticated production proof exists for ANY role. One owner
   decision unblocks the production-proof half of W6/W7/W8/W11/W12.
2. **`booking_requests` = 0 rows in prod** — the marketplace loop has never
   closed once with a real user.
3. **`usage_cost_events` production-ahead-of-main drift** — Supabase Preview
   CI red; `docs/audits/usage-cost-migration-drift-inventory-2026-08-03.md`.
4. **Ledger "Deferred" list stale** — still lists `20260714210000_company_memberships_v1`
   although the applied successor (`20260806090000`) exists.

## The matrix

| W | Purpose | State (verified) | Merged PRs | Prod schema | Proof | Exact remaining gaps | Class | Pre-pilot? | Next executable slice |
|---|---|---|---|---|---|---|---|---|---|
| W1 | Product/UI audit + landing repair + journal→capability loop | Complete; evidence dir with 4 browser PNGs | premium-rebuild integration | none | browser ✔ | none | **DONE** | no | — |
| W2 | Design tokens + shell | Ledger says complete; superseded in practice by visual system S1–S3 (`visual-contract-v1.md`). `ResultShell.tsx` + skeleton primitive NOW exist (closing two 08-03 baseline findings) | #996 #997 #999 #1000 #1005 | none | S2/S3 evidence | 16 distinct `*card.tsx`; 4 player-card variants + 2 rings; 2 empty-state components for ~70 routes (ratchet-guarded) | **PARTIAL + NOT_LAUNCH_CRITICAL** | no | ratchet `card-border` count DOWN via `visual-contract-v1.test.ts` |
| W3 | Chat-first workspace consolidation | Complete, FROZEN; `dashboard/advanced` = 0 files on main; −7,487 LOC | (pre-#968 train) | none | browser ✔ + deploy | none | **DONE** | no | — |
| W4 | Professional identity (Player Card) | Complete, FROZEN at `426e87aa` | (W4 train) | none | browser ✔ | 9 owner-gated items (`w4-acceptance.md` §4) | **DONE + OWNER_GATED remainder** | no | owner reviews the 9-item list |
| W5 | Journal / evidence / skills | Complete-frozen at `7621acab` with carried gaps | #968–#971 | none | browser ✔ | clarify-flow is a write-only sink; `candidate_skills` write path unaudited; obsolete `journal.visibility_scope`; Draft #867 unmerged; prod has only 2 verified skills | **PARTIAL (frozen ⇒ DEFERRED_LONG_TERM)** | no | close the clarify-flow sink (2 files, no migration) |
| W6 | Trust / experience domain | **Schema ACTIVE in prod** (applied 2026-08-04, ledger 173→174); `experiences` result = `real`; local full cycle proven. **Author/subject slice CODE COMPLETE 2026-08-06**: org-scoped bookings resolve organization subjects, `author_side` model added, moderation queue names subject+author side; DB proof 35/35 + v1 regression 43/43 + rollback cycle + browser 9/9 (`docs/human-gates/experience-author-subject-v1-gate.md`) | #972–#974 #977 #1008 + author/subject PR #1037 | `20260802120000` APPLIED; `20260806230000` **APPLIED 2026-08-06 13:56 UTC (W6-D1), ledger 184→185, version `20260806135649`** | local ✔ / prod write **pending PROD_QA execution (approved)** | production write proof via the approved PROD_QA journey | **SCHEMA COMPLETE + APPLIED — write proof in flight** | no | run the approved PROD_QA journey |
| W7 | Employee journey | ~88%; zero P0; P1-1/#981, P1-2/#979 closed; **P1-4 inventory DONE**; **W7-S1 SHIPPED**; **W7-S3 SHIPPED**; **W7-S2 SHIPPED**; **W7-S4 SHIPPED**; **W7-S5 SHIPPED** | #979 #981 #1051 #1052 #1053 | none own | local journey ✔ (83 shots) + profile inventory (`docs/audits/W7_PROFILE_FULL_INVENTORY.md`) + W7-S1 before/after (`W7_S1_PROFILE_HUB_OVERVIEW.md`) + W7-S3 six DOM fingerprints, zero product-visible differences (`W7_S3_PROFILE_READ_WATERFALL.md`) + **W7-S2 19/19 keyboard+persistence checks at 1440 and 375, ARIA-tree scan** (`W7_S2_PROFILE_ACCESSIBILITY.md`) + **W7-S4 before/after on BOTH affected routes, 2 identity scenarios, 1440+375, 16 screenshots** (`W7_S4_PROFILE_INFORMATION_ARCHITECTURE.md`) | **W7-S1 DONE** — five duplicate readiness summaries → ONE `ProfileHubOverview`; height −13.6…−19.0% desktop / −14.3…−19.1% mobile; overview moved 4th block→1st. **W7-S3 DONE** — read waterfall 43→9 serial stages, 17→5 sequential DB round trips; behaviour-identical (fingerprint-proven); local timing INCONCLUSIVE and stated as such. **W7-S2 DONE** — `worker-availability-prefs-form` sub-44px 36→0, tabbable radios per group 3→1 with Arrow/Home/End, selection no longer colour-only; page-wide sub-44px 71→35. CORRECTION: the audit's "34 unlabelled inputs" counted `input[type=hidden]`; the honest page-wide figure is **2**, neither in that form. PROFILE simplicity 1.0→1.86→**2.14** (still NEEDS_POLISH, not PASS). **W7-S4 DONE** — `#managed-companies` + `MessageButton` moved to `/dashboard/network`; profile height −197…−256px, cards 16→15, `<h2>` 12→11, A-1 unlabelled perceivable inputs **2→0**, sub-44px **36→29**. CORRECTION: BOTH destinations named by W7-S1 §11 were rejected on evidence — `/dashboard/company` is role-gated and single-company (it redirects the zero-company reader the add-company action exists for), and `/dashboard/communication` is forbidden a contact button by the counterpart-trust P0 guard, which was KEPT. `/dashboard/network` already rendered the same `getOwnedOrganizations()` list, so `#managed-companies` was a duplicate; 3 capabilities it had and network lacked (add-company action, zero-company state, individual note) were ported — network is now strictly more capable. REMAINING: W7-S5 non-worker identity + v1/v2 first-vs-third-person copy; A-2 28–29 sub-44px targets still on the profile; `MessageButton` ~26px at every call site; **P1-3 conversation memory = BLOCKED/MIGRATION_GATE** — reality-audited: the AI-control thread has NO persistence of any kind (plain `useState`), no `assistant_*` table exists, and the package is Draft PR #883 (proposal SHA-256 re-verified INTACT; note the working-tree hash mismatches on Windows are CRLF, not tampering). **P2-1 = PARTIAL** — mechanism found exactly: `coalesce(expected_end_date, start_date)` collapses an end-less booking to its START DAY, so double-booking protection is absent from day two; the honesty half SHIPPED (form now states an empty end books one day, `aria-describedby`), the capability half is owner-gated and needs 5 product definitions first. See `docs/audits/W7_CLOSURE_AUDIT.md`. **SAFE-WORK CLOSURE (2026-08-07, PRs #1057 #1058 #1060 #1061 + infra #1059)**: all four safe items CLOSED — W7-S5b shipped with the premise CORRECTED (the "12 of 21 lost sections" state is unreachable post-0009; the real defect was no-person-ROLE accounts reading a worker-framed page, fixed with THE identity notice, both presentations browser-proven); A-2 → 0 enabled sub-44px controls in the worker sections with forms OPEN (sweep-proven) + 7 new open-form inputs named; transport copy reconciled (schema was deliberately distinct, copy was the defect — guard pins both directions); `marketplaceHub` retired (3 live keys → `network.organizations.*`, 14 dead keys deleted). See `docs/audits/W7_SAFE_WORK_CLOSURE.md` | **W7_SAFE_TECHNICAL_WORK_COMPLETE_OWNER_GATES_REMAIN** | no | owner: decide P1-3 (#883 apply, MIGRATION_GATE) and P2-1 (pick a model from `docs/program/W7_P2_1_OPEN_ENDED_BOOKING_DECISION_PACKAGE.md`, PRODUCT_DECISION — recommended: Model A review-date). No safe W7 work remains |
| W8 | Employer journey / chat workspace | Was 20%; **materially advanced**: #1006 gave the chat a real `candidates` result + callers; #1007 made assign-worker reachable; #1027/#1032 fixed non-owner members' `company-not-owned`; org demand spine APPLIED | #978 #1006 #1007 #1017 #1027 #1031 #1032 #1033 | via M-P0-6 `20260806200000` APPLIED | local ✔ | agency actions unscoped; drafts are PERSON-scoped under an ORG room (owner decision, §6 of the IA audit) | **PARTIAL** | **yes** | **employer analytics v1 DONE 2026-08-07 (#1063)** — org demand rollup on the existing /dashboard/reports org view (no new route: A-09), plus the W14-7 remainder (getCompanyDemandIntelligence widened profile_id→organization_id). Time-to-fill ships as an explicit unmeasured state (0 booking_requests in prod). See `docs/audits/W8_ORG_DEMAND_ROLLUP_V1.md`. **`/dashboard/candidates` IA RECONCILED 2026-08-07** — and re-homing was REJECTED on evidence: the route and its own page were already honest (`candidate_drafts`, `owner_id = auth.uid()`, no account / no verification / no matching / not assignable). The misnaming lived entirely in the WRAPPERS: the planning chip said "Browse candidates", the room said "Review candidates and shortlists", and — the real defect — the room promised to "start a conversation" with someone who by construction has no account, contradicting the page's own honesty note three sections below. Correction = rename the visible concept + clarify framing (A+C), route contract untouched, no privacy widening, no second candidate system. The audit also names the two DISTINCT "candidates" concepts that had been colliding (private drafts vs `/dashboard/company/scouting` supply) and records the person-vs-org scope tension as an owner decision. See `docs/audits/W8_CANDIDATES_INFORMATION_ARCHITECTURE.md` |
| W9 | Organizations & teams | **Structurally closed by the M-P0 train**: ownership cap removed, memberships v1 + commands + authority widening + durable workspace + consumer slice ALL applied/merged; §5 16-assertion journey PASS (#1034) | #975 #980 #983 + #1019 #1021–#1024 #1026 #1027 #1032 #1034 | 5 M-P0 migrations APPLIED (see APPLIED_LEDGER) | local ✔ ×4 proofs / prod schema ✔ | **read-path `getOwnCompany()` CLOSED 2026-08-07 (#1066)** — and it was not cosmetic: the owned-only reader named the WRONG organization for a non-owner manager (valid member pointer fell through to the single owned org); layout now derives name/id/list from the ONE workspace context, fail-closed validation unchanged. housekeeping: deferred-list entry for old `20260714210000` stale | **near-DONE** | no | housekeeping only |
| W10 | Marketplace & matching | Was 45%; P0-1/#986, P0-2/#989, P0-3/#992, P1-1/#1001 (canonical demand truth) closed; P1-3 org scope UNBLOCKED (spine applied) | #986 #989 #992 #1001 #1033 | consumes `20260806200000` APPLIED | local ✔ (14/14 two-org proof) | match chips report profile completeness, not match facts (P1-2); dead geography (no coordinates/geocoder); **W10-7 symmetry guard WRITTEN 2026-08-07 (#1064)** — and it records a live finding: the reduced role-text need never evaluates `engagement_form`, so the worker board can report `strong` for a demand whose own payload BLOCKS it in scouting. Pinned, not fixed | **PARTIAL** | no | decide P0-1 remainder: make the worker board use `needFromDemandRow` everywhere, or state the reduction in the UI |
| W11 | Project operating system | Was 25%; both schema blockers CLOSED (assigned-worker read + lifecycle APPLIED); `project` result = `real` (#1007); **F7 CLOSED 2026-08-07 (#1068)** | #985 #988 #1002 #1007 #1068 | `20260803090000` + `20260804120000` APPLIED | local ✔ / prod: 5 projects all `draft` | **F7 CLOSED — and the deep-link framing was imprecise.** The route was never missing; two ARRIVAL paths were. (a) The chat-first `project` result let a manager pick a project, then offered ONE exit — to `/dashboard/projects`, the LIST — dropping the identity just chosen. The detail exit is now project-scoped and chooses its surface by the SERVER's `canManage` (`can_manage_project` on the contract): manager → the operating centre, otherwise → the role-aware project page. The PICKER keeps the list, because at depth 0 no project is chosen. (b) The stadium's link was the LAST element of a page rendering 7 sections above it (~1340px desktop / ~1790px mobile); it moved into the header — measured 207px / 223px, above the fold at both sizes, one link not two. Offering a route is never a grant: unknown project id → `ops-not-authorized` with no board; worker identity → no board. Browser proof 24/24 at 1440+375, no URL typed in it, click AND keyboard, 44px, no overflow, no console/hydration errors. HONEST LIMIT: the local fixture holds ONE company with projects, so a two-org cross-org proof was NOT available and is NOT claimed. REMAINING: dismissed-manager rights must consume the applied membership authority; lifecycle never run by a real user | **PARTIAL** | **yes** | production completion proof (lifecycle run by a real user); dismissed-manager authority consumption |
| W12 | Calendar & conflicts | Core guard APPLIED (row lock + advisory + EXCLUDE gist); 21/21 concurrency proof local; §5 journey proved the CROSS-COMPANY conflict live | #976 #982 #1003 | `20260802150000` APPLIED | local ✔ / prod race unexercised (0 bookings) | **9 calendar sources with NO model** (availability, shifts, vacation, sick, meetings, holidays, service-order TEXT dates, instruction due dates, travel) — each additive schema + UI, owner-gated | **PARTIAL** | **yes** (proof) | source #7: real date columns on `customer_requests` (only source with an existing model) |
| W13 | Communication & attention | **BASELINE DEFINED 2026-08-07 from shipped code** — `docs/program/W13_COMMUNICATION_ATTENTION_BASELINE.md`. W13 was never greenfield: 8 signals ship through ONE request-cached source (`getSpineCounts`) feeding FIVE consumers (bell, nav badges, module cards, `/dashboard/activity`, the assistant), so they cannot disagree. First principle adopted from the code itself: **a badge that cannot clear is permanent noise, not a signal** | — | `worker_opportunity_seen` + `demand_interest_seen` deferred (owner-gated) | every signal's source/audience/context/count/clearing/fail-closed behaviour documented per row | **W13-1** `ConfirmPulse` is a 9th REAL signal (gated `reviewable_journal_entry_ids` RPC, clears at `/dashboard/inbox/quick`) that is NOT in the spine — it renders on 3 project surfaces only, so a waiting confirm queue announces itself only if you open a project. **The wiring was ATTEMPTED in this slice and correctly REFUSED by the guards**, which surfaced an undocumented THIRD admission rule: `activity-centre.ts` inverts the module registry's `attentionSignalIds`, so a spine signal must also be DECLARED BY A DASHBOARD MODULE or it vanishes from `/dashboard/activity`. No module hosts the confirm queue (`journal` is `roles:["worker"]`, but confirming is a MANAGER act), so W13-1 needs one IA answer, not just wiring. Code left unchanged. **W13-6** the spine is PERSON-scoped: no reader takes the active workspace, so post-W9 a multi-org manager sees ONE merged bell with no organisation label — correct for invitations/messages, undecided for org-bound work signals. **W13-5** no notification email and no push exist at all (`lib/email/transactional.ts` is an INVITE-only adapter). Bounded stale-risk: the unread scan is `.limit(500)` newest-first. Ledger gap: `20260627181500_service_requests_seen` has NO ledger entry either way. Stale comment: `spine-signals.ts` still calls `work_tasks` unapplied — it was APPLIED 2026-07-11, so `open-task-attention` is a live count, not a dead one. NO duplicated signal found — anti-double-counting is already reasoned about per module | **BASELINE DEFINED — scope no longer undefined** | no | **safe with zero owner input: W13-0b (stale comment), W13-7 (panel a11y pass)**. Owner-gated: **W13-1 needs a host-module decision** (new module card vs widening an existing one — Product Gate A-09 applies); apply the 2 seen migrations; decide W13-6 filter-vs-label; decide whether an email/push channel exists at all |
| W14 | Analytics & KPI | Was 25%; P0s closed; #1015 (11 mid-funnel events + cost writer + admin AI-cost view) + #1031 (org attribution seam) landed | #984 #987 #990 #991 #1015 #1031 | `ai_runs` APPLIED (0 rows, AI disabled); `usage_cost_events` APPLIED (drift item) | admin surfaces live | ~10 of 13 slices open; 5 charts with no host; no `dashboard_viewed` emitter; server rows hardcode `locale:"lt"`; **90-day `ai_runs` retention REQUIRED before enabling any AI provider** | **PARTIAL** | no | W14-6 employer analytics org-scoped (prereq #1031/#1033 landed) |
| W15 | — | **NO DEFINITION EXISTS** (independently re-grepped) | — | — | — | the scope itself | **REMAINING (undefined)** | no | define or drop the number |
| W16 | "Commercial" (one-line hint in W8's audit) | Superseded by **M-P0-7** + Stripe TEST v2 (#1030 merged; #1035 Draft owner-gated; #844 closed superseded) | #1014 #1030; Draft #1035 | `20260806220000` UNAPPLIED (owner gate) | unit/guard ✔ | apply the v2 schema (gate); TEST credentials (owner); **Stripe Live NOT AUTHORIZED** | **SUPERSEDED + OWNER_GATED** | no (test mode only) | owner: env package `docs/billing/STRIPE_TEST_ENV_OWNER_PACKAGE.md` |
| W17–W22 | — | **NO DEFINITION EXISTS** — the baseline itself says "an undefined stage cannot be planned against" | — | — | — | the scope itself | **REMAINING (undefined)** | no | define or drop the numbers |

## First genuinely incomplete W (launch-relevant, executable now)

**W6 — Trust / experience domain.** W1/W3 are done; W2 is guard-ratcheted
polish (not launch-critical); W4's remainder and W5 are frozen/owner-gated.
W6 is schema-active with a NAMED modelling defect (employer-subject
experiences stored as worker-subject) whose fix is pure code+gated-migration
work, plus a production write proof waiting only on the PROD_QA owner gate.
The sequential train (`SEQUENTIAL_W_EXECUTION_TRAIN.md`) therefore starts at
W6.
